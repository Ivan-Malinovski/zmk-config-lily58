# Dongle central configuration

# USB output (dongle connects to host via USB)
CONFIG_ZMK_USB=y

# BLE for connecting to peripheral halves
CONFIG_ZMK_BLE=y

# Two peripherals (left + right)
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2
CONFIG_BT_MAX_CONN=7
CONFIG_BT_MAX_PAIRED=7
